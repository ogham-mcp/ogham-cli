// Tailwind v4 auto-detects templates via the @source directive in CSS,
// but we leave a config stub for editors that expect one.
export default {
  content: [
    "../templates/**/*.templ",
    "../templates/**/*_templ.go"
  ]
};
