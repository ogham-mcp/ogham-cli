# Build artifacts
ogham
dist/
*.exe
*.test

# Test / profiling output
cover.out
cover.html
coverage.out
coverage.html
*.prof

# macOS noise
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Local environment files (never commit)
.env
.env.local
.coverage/

# Dashboard build artefacts: the compiled styles.css ships in the repo
# (it's a //go:embed artefact), but the Bun install cache + lockfile do
# not need to be tracked. See internal/dashboard/_build/README.md.
internal/dashboard/_build/node_modules/
internal/dashboard/_build/bun.lock

# Local-only progress / tracking notes (never commit)
docs/tracking*.md
