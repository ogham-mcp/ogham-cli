# Build artifacts
ogham
dist/
*.exe
*.test

# Test / profiling output
cover.out
cover.html
coverage.out
coverage.html
*.prof

# macOS noise
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Local environment files (never commit)
.env
.env.local
.coverage/

# Dashboard build artefacts: the compiled styles.css ships in the repo
# (it's a //go:embed artefact), but the Bun install cache + lockfile do
# not need to be tracked. See internal/dashboard/_build/README.md.
internal/dashboard/_build/node_modules/
internal/dashboard/_build/bun.lock

# Local-only progress / tracking notes (never commit)
docs/tracking*.md
# Internal scope / planning packages. Working material addressed to the
# dev fleet, referencing Linear tickets and internal process -- not
# documentation for users of a public repo. One was published by
# accident in 0ef9320 via `git add -A`; this stops the next one.
docs/scope/
docs/release-v*.md
